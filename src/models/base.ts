export enum DateType {
  TODAY = "today",
  TOMORROW = "tomorrow",
  NEXT_WEEK = "next_week",
  OTHER_TIME = 'other_time',
  LATER_TODAY = "later_today",
}

export enum RepeatType {
  DAILY = 'daily',
  WEEKDAYS = 'weekdays',
  WEEKLY = 'weekly',
  MONTHLY = 'monthly',
  YEARLY = 'yearly',
  // CUSTOM = 'Custom' TODO
}