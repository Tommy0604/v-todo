export enum DateType {
  TODAY = "today",
  TOMORROW = "tomorrow",
  NEXT_WEEK = "next week",
  OTHER_TIME = 'other time',
  LATER_TODAY = "later today",
}

export enum RepeatType {
  DAILY = 'Daily',
  WEEKDAYS = 'Weekdays',
  WEEKLY = 'Weekly',
  MONTHLY = 'Monthly',
  YEARLY = 'Yearly',
  // CUSTOM = 'Custom' TODO
}